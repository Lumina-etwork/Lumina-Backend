{
  "name": "vesting-backend",
  "version": "1.0.0",
  "description": "API for Vesting Vault",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:integration": "jest --testPathPattern=integration",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "lint:ci": "eslint . --ext .js --format=junit --output-file=reports/junit.xml",
    "docker:build": "docker build -t vesting-backend .",
    "docker:run": "docker-compose up -d",
    "docker:stop": "docker-compose down",
    "docker:test": "docker-compose -f docker-compose.test.yml up --abort-on-container-exit",
    "security:audit": "npm audit --audit-level moderate",
    "security:scan": "npm audit --json > reports/audit.json || true"
  },
  "dependencies": {
    "@hapi/hapi": "^21.4.8",
    "axios": "^1.6.2",
    "cors": "^2.8.6",
    "crypto": "^1.0.1",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "node-vault": "^0.12.0",
    "pg": "^8.11.3",
    "reflect-metadata": "^0.1.13",
    "stellar-sdk": "^11.1.0",
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3"
  }
}
